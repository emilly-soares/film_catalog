import { useEffect, useState, useCallback, useMemo, useRef, ChangeEvent } from "react";
import './App.css'

interface EditMovie {
  enabled: boolean;
  movieTitle: string;
}

interface Movie {
  id: number;
  title: string;
  poster: string;
  synopsis: string;
  cast: string;
  rating: number;
  category: string;
  premiere: string;
  duration: number;
}


function App() {

  return (
  
  )
}

export default App;
